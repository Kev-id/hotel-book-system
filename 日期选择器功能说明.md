# 日期选择器功能说明

## 更新内容

在List页和Detail页添加了入住时间段查询功能，用户可以在这些页面修改日期并重新查询价格。

## 功能位置

### 1. List页（酒店列表页）

**位置**: 高级筛选表单中

**功能**:
- 显示日期范围选择器
- 与其他筛选条件（城市、价格、星级）一起使用
- 修改日期后点击"筛选"按钮重新查询
- 点击"重置"按钮清除所有筛选条件（包括日期）

**特点**:
- 日期选择器集成在筛选表单中
- 支持与其他筛选条件组合使用
- 日期变更后需要点击"筛选"按钮才会生效

### 2. Detail页（酒店详情页）

**位置**: 页面顶部，返回按钮下方

**功能**:
- 显示日期范围选择器
- 显示入住天数
- 修改日期后自动重新计算价格
- 实时更新所有房型的价格

**特点**:
- 独立的日期选择卡片
- 日期变更后自动生效（无需点击按钮）
- 显示入住天数提示

## 使用流程

### 场景1: 从首页搜索

1. 用户在首页选择城市和日期
2. 点击"查询酒店"
3. 进入List页，日期自动回显在筛选表单中
4. 价格显示为选定日期的总价和最低价

### 场景2: 在List页修改日期

1. 用户在List页的筛选表单中修改日期
2. 点击"筛选"按钮
3. 页面重新加载，显示新日期的价格
4. URL参数更新，包含新的日期

### 场景3: 在Detail页修改日期

1. 用户点击酒店进入详情页
2. 日期自动从URL参数中读取并显示
3. 用户修改日期选择器
4. 页面自动重新计算所有房型的价格
5. 价格卡片和房型列表实时更新

### 场景4: 清除日期

**List页**:
- 点击"重置"按钮清除所有筛选条件
- 或手动清除日期选择器（点击×图标）
- 点击"筛选"按钮应用

**Detail页**:
- 点击日期选择器的×图标清除日期
- 价格自动恢复为基础价格显示

## 技术实现

### URL参数管理

日期通过URL参数传递：

```
/list?city=beijing&checkIn=2026-02-11&checkOut=2026-02-14
/detail/1?checkIn=2026-02-11&checkOut=2026-02-14
```

**优势**:
- 支持浏览器前进/后退
- 支持书签和分享
- 页面刷新后保持状态

### List页实现

```jsx
// 表单提交时更新URL
const handleFilterSubmit = (values) => {
  const params = new URLSearchParams(location.search);
  
  if (values.dateRange && values.dateRange.length === 2) {
    params.set('checkIn', values.dateRange[0].format('YYYY-MM-DD'));
    params.set('checkOut', values.dateRange[1].format('YYYY-MM-DD'));
  }
  
  navigate(`?${params.toString()}`);
};

// 表单回显
useEffect(() => {
  const { checkIn, checkOut } = parseSearchParams();
  
  if (checkIn && checkOut) {
    form.setFieldsValue({
      dateRange: [dayjs(checkIn), dayjs(checkOut)]
    });
  }
}, [location.search]);
```

### Detail页实现

```jsx
// 日期变更时自动更新URL
const handleDateChange = (dates) => {
  setDateRange(dates);
  
  if (dates && dates.length === 2) {
    const params = new URLSearchParams(location.search);
    params.set('checkIn', dates[0].format('YYYY-MM-DD'));
    params.set('checkOut', dates[1].format('YYYY-MM-DD'));
    navigate(`?${params.toString()}`, { replace: true });
  }
};

// 监听URL变化重新计算价格
useEffect(() => {
  const { checkIn, checkOut } = getSearchParams();
  
  if (checkIn && checkOut) {
    setDateRange([dayjs(checkIn), dayjs(checkOut)]);
  }
  
  fetchHotelDetail();
}, [id, location.search]);
```

## 用户体验

### 1. 日期限制

- 禁止选择过去的日期
- 只能选择今天及以后的日期
- 使用 `disabledDate` 属性实现

```jsx
<RangePicker
  disabledDate={(current) => current && current < dayjs().startOf('day')}
/>
```

### 2. 日期格式

- 统一使用 `YYYY-MM-DD` 格式
- 前端显示：2026-02-11
- URL参数：checkIn=2026-02-11
- 后端接收：2026-02-11

### 3. 视觉反馈

**List页**:
- 日期选择器在筛选表单中
- 与其他筛选条件对齐
- 使用日历图标标识

**Detail页**:
- 独立的卡片样式
- 显示入住天数
- 日历图标 + 文字说明
- 响应式布局

### 4. 状态保持

- 页面刷新后日期保持
- 前进/后退按钮正常工作
- 从List页进入Detail页，日期自动传递
- 从Detail页返回List页，日期保持不变

## 价格计算逻辑

### 有日期时

```
总价 = 每天价格之和
每晚低至 = 入住期间的最低价格
入住天数 = 退房日期 - 入住日期
```

### 无日期时

```
显示房型基础价格
不显示总价和入住天数
```

## 示例场景

### 示例1: 完整流程

```
1. 首页选择：
   - 城市：北京
   - 入住：2026-02-11
   - 退房：2026-02-14
   - 点击"查询酒店"

2. List页显示：
   - URL: /list?city=beijing&checkIn=2026-02-11&checkOut=2026-02-14
   - 筛选表单回显日期
   - 酒店卡片显示：
     * 总价: ¥3,996
     * 每晚低至: ¥888

3. 点击酒店进入Detail页：
   - URL: /detail/1?checkIn=2026-02-11&checkOut=2026-02-14
   - 日期选择器显示：2026-02-11 到 2026-02-14
   - 显示：共 3 晚
   - 每个房型显示总价和详情

4. 修改日期：
   - 选择：2026-02-15 到 2026-02-18
   - URL自动更新
   - 价格自动重新计算
```

### 示例2: 修改日期

```
List页修改日期：
1. 修改日期选择器
2. 点击"筛选"按钮
3. 页面重新加载
4. 显示新日期的价格

Detail页修改日期：
1. 修改日期选择器
2. 自动重新计算（无需点击按钮）
3. 价格实时更新
```

## 注意事项

### 1. 性能考虑

- Detail页日期变更时会触发多次API调用（每个房型一次）
- 使用 `replace: true` 避免历史记录堆积
- 考虑添加防抖优化

### 2. 错误处理

- 日期选择器清除时，清空价格映射
- API调用失败时，使用基础价格
- 显示友好的错误提示

### 3. 移动端适应

- 日期选择器响应式布局
- 移动端使用原生日期选择器
- 触摸友好的交互

## 相关文件

### 修改的文件

- `src/pages/client/List/index.jsx` - 添加日期选择器到筛选表单
- `src/pages/client/Detail/index.jsx` - 添加独立的日期选择卡片

### 依赖

- `dayjs` - 日期处理库
- `antd` - DatePicker, RangePicker组件

## 测试建议

### 测试用例

1. ✅ 从首页带日期进入List页，日期正确回显
2. ✅ 在List页修改日期，价格正确更新
3. ✅ 从List页进入Detail页，日期正确传递
4. ✅ 在Detail页修改日期，价格自动更新
5. ✅ 清除日期后，显示基础价格
6. ✅ 浏览器前进/后退，日期状态正确
7. ✅ 页面刷新后，日期保持不变
8. ✅ 禁止选择过去的日期
9. ✅ 日期格式正确（YYYY-MM-DD）
10. ✅ 移动端显示正常

## 总结

通过在List页和Detail页添加日期选择器，用户现在可以：

- ✅ 在任何页面修改入住日期
- ✅ 实时查看不同日期的价格
- ✅ 灵活比较不同时间段的价格
- ✅ 享受流畅的用户体验

这大大提升了用户的使用便利性和价格透明度。

---

更新人: Kiro AI Assistant
更新日期: 2026-02-11
版本: v1.2.0

# 🗄️ 数据库初始化指南

**适用场景**: 新同事第一次搭建项目环境

---

## 📋 目录

1. [完整初始化流程](#完整初始化流程)
2. [分步骤说明](#分步骤说明)
3. [验证数据库](#验证数据库)
4. [常见问题](#常见问题)

---

## 完整初始化流程

### 🚀 一键初始化（推荐）

如果是全新环境，按顺序执行以下命令：

```bash
cd backend

# 第1步：创建数据库和基础表（必须！）
node sql/init.js

# 第2步：运行数据库迁移（必须！）
node sql/migrate.js

# 第3步：AI功能表迁移（必须！）
node sql/migrate-ai-review.js
node sql/migrate-favorite-compare.js
node sql/migrate-analytics-indexes.js

# 第4步：修复收藏表AI字段（必须！）
node fix-favorites-ai-reason.js

# 第5步：验证数据库
node check-hotel-prices.js
```

### ⚡ 快速复制命令

```bash
cd backend && node sql/init.js && node sql/migrate.js && node sql/migrate-ai-review.js && node sql/migrate-favorite-compare.js && node sql/migrate-analytics-indexes.js && node fix-favorites-ai-reason.js && node check-hotel-prices.js
```

---

## 分步骤说明

### 第1步：创建数据库和基础表

```bash
cd backend
node sql/init.js
```

**这个脚本会做什么**：
- ✅ 创建数据库 `hotel_booking`
- ✅ 创建基础表：
  - `users` - 用户表
  - `hotels` - 酒店表
  - `room_types` - 房型表
  - `price_calendar` - 价格日历表
- ✅ 插入测试数据：
  - 5个用户（admin1, merchant1, 陈凯文, icc, user1）
  - 4家酒店（北京、上海、广州、深圳）
  - 8种房型（每家酒店2种）
  - 价格日历测试数据

**预期输出**：
```
✓ 数据库 hotel_booking 创建成功
✓ users 表创建成功
✓ hotels 表创建成功
✓ room_types 表创建成功
✓ price_calendar 表创建成功
✓ 用户数据插入成功
✓ 酒店数据插入成功（4家）
✓ 房型数据插入成功（每个酒店2种房型）
✓ 价格日历数据插入成功（12 条记录）
✅ 数据库初始化完成！
```

---

### 第2步：运行基础迁移

```bash
node sql/migrate.js
```

**这个脚本会做什么**：
- ✅ 添加 `tags` 列到 hotels 表
- ✅ 添加 `description` 列到 hotels 表
- ✅ 添加 `rejectReason` 列到 hotels 表
- ✅ 更新现有酒店数据

**预期输出**：
```
✓ 添加 tags 列成功
✓ 添加 description 列成功
✓ 添加 rejectReason 列成功
✓ 更新现有数据成功
✅ 数据库迁移完成！
```

---

### 第3步：AI功能表迁移

#### 3.1 AI评论系统表

```bash
node sql/migrate-ai-review.js
```

**创建的表**：
- `ai_review_cache` - AI评论缓存表
- `reviews` - 评论表（如果不存在）
- `orders` - 订单表（如果不存在）

**新增字段**：
- `reviews.ai_reply` - AI回复内容
- `reviews.reply_time` - 回复时间

**预期输出**：
```
✓ 创建 ai_review_cache 表成功
✓ 创建 reviews 表成功
✓ 创建 orders 表成功
✓ 添加 ai_reply 字段成功
✓ 添加 reply_time 字段成功
✅ AI评论系统表迁移完成
```

#### 3.2 收藏对比系统表

```bash
node sql/migrate-favorite-compare.js
```

**创建的表**：
- `favorites` - 收藏表

**新增字段**：
- `favorites.ai_reason` - AI推荐理由
- `favorites.created_at` - 创建时间

**新增索引**：
- `idx_favorites_user` - 用户索引
- `idx_favorites_hotel` - 酒店索引

**预期输出**：
```
✓ 创建 favorites 表成功
✓ 添加 ai_reason 字段成功
✓ 添加 created_at 字段成功
✓ 创建索引 idx_favorites_user
✓ 创建索引 idx_favorites_hotel
✅ 收藏对比系统表迁移完成
```

#### 3.3 数据看板索引

```bash
node sql/migrate-analytics-indexes.js
```

**创建的索引**：
- `idx_orders_merchant_time` - 订单商户时间索引
- `idx_orders_analytics` - 订单分析索引
- `idx_reviews_hotel_time` - 评论酒店时间索引

**预期输出**：
```
✓ 创建索引: idx_orders_merchant_time
✓ 创建索引: idx_orders_analytics
✓ 创建索引: idx_reviews_hotel_time
✅ Analytics索引创建完成
```

---

### 第4步：修复收藏表AI字段

```bash
node fix-favorites-ai-reason.js
```

**这个脚本会做什么**：
- ✅ 检查 `favorites` 表是否存在 `ai_reason` 字段
- ✅ 如果不存在，添加该字段
- ✅ 验证字段添加成功

**预期输出**：
```
✓ ai_reason 字段已存在
✅ 收藏表AI字段检查完成
```

或

```
✓ 添加 ai_reason 字段成功
✅ 收藏表AI字段修复完成
```

---

### 第5步：验证数据库

```bash
node check-hotel-prices.js
```

**这个脚本会做什么**：
- ✅ 检查所有酒店的数据完整性
- ✅ 显示哪些酒店数据完整
- ✅ 显示哪些酒店缺少数据

**预期输出**：
```
📊 酒店数据检查报告
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ 数据完整的酒店（可用于对比测试）:
  1. 北京国际大饭店 (ID: 1)
  2. 上海外滩华尔道夫酒店 (ID: 2)
  3. 广州白天鹅宾馆 (ID: 3)
  4. 深圳瑞吉酒店 (ID: 4)

⚠️ 数据不完整的酒店:
  （暂无）

📈 统计:
  - 总酒店数: 4
  - 数据完整: 4
  - 数据不完整: 0
  - 完整率: 100%
```

---

## 验证数据库

### 方式1：使用MySQL命令行

```bash
# 连接数据库
mysql -u root -p hotel_booking

# 查看所有表
SHOW TABLES;

# 查看用户数据
SELECT * FROM users;

# 查看酒店数据
SELECT id, name, city, status FROM hotels;

# 查看房型数据
SELECT * FROM room_types;

# 查看价格日历
SELECT * FROM price_calendar LIMIT 10;

# 退出
EXIT;
```

### 方式2：使用测试脚本

```bash
cd backend

# 测试所有功能
node test-all-features.js

# 测试AI功能
node test-ai-api.js

# 测试收藏功能
node test-favorite-api.js

# 测试数据看板
node test-analytics-api.js
```

---

## 常见问题

### Q1: 数据库连接失败？

**错误信息**：
```
Error: connect ECONNREFUSED 127.0.0.1:3306
```

**解决方案**：
1. 确认MySQL服务已启动
2. 检查 `backend/.env` 配置：
   ```bash
   DB_HOST=localhost
   DB_USER=root
   DB_PASSWORD=你的密码
   DB_NAME=hotel_booking
   ```

### Q2: 表已存在错误？

**错误信息**：
```
Error: Table 'users' already exists
```

**解决方案**：
这是正常的！脚本使用 `CREATE TABLE IF NOT EXISTS`，会自动跳过已存在的表。

### Q3: 数据库已存在，想重新初始化？

**方案1：删除数据库重建**
```bash
# 连接MySQL
mysql -u root -p

# 删除数据库
DROP DATABASE hotel_booking;

# 退出
EXIT;

# 重新运行初始化
cd backend
node sql/init.js
```

**方案2：只删除数据**
```bash
# 连接MySQL
mysql -u root -p hotel_booking

# 清空表数据（保留表结构）
TRUNCATE TABLE price_calendar;
TRUNCATE TABLE room_types;
TRUNCATE TABLE orders;
TRUNCATE TABLE reviews;
TRUNCATE TABLE favorites;
TRUNCATE TABLE hotels;
TRUNCATE TABLE users;

# 退出
EXIT;

# 重新运行初始化
cd backend
node sql/init.js
```

### Q4: 权限不足错误？

**错误信息**：
```
Error: Access denied for user 'root'@'localhost'
```

**解决方案**：
1. 确认MySQL密码正确
2. 检查用户权限：
   ```sql
   GRANT ALL PRIVILEGES ON hotel_booking.* TO 'root'@'localhost';
   FLUSH PRIVILEGES;
   ```

### Q5: 字符集问题（中文乱码）？

**解决方案**：
```sql
-- 设置数据库字符集
ALTER DATABASE hotel_booking CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 设置表字符集
ALTER TABLE hotels CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
ALTER TABLE users CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### Q6: 迁移脚本运行顺序错了？

**正确顺序**：
```bash
1. init.js           # 必须第一个运行
2. migrate.js        # 第二个
3. migrate-ai-review.js
4. migrate-favorite-compare.js
5. migrate-analytics-indexes.js
6. fix-favorites-ai-reason.js
```

如果顺序错了，可能会出现表不存在的错误。解决方案：按正确顺序重新运行。

---

## 📊 数据库结构概览

### 核心表

| 表名 | 说明 | 记录数 |
|------|------|--------|
| users | 用户表 | 5 |
| hotels | 酒店表 | 4 |
| room_types | 房型表 | 8 |
| price_calendar | 价格日历表 | 12+ |
| orders | 订单表 | 动态 |
| reviews | 评论表 | 动态 |
| favorites | 收藏表 | 动态 |
| ai_review_cache | AI缓存表 | 动态 |

### 测试账号

| 角色 | 用户名 | 密码 | ID |
|------|--------|------|-----|
| 管理员 | admin1 | 123456 | 1 |
| 商家 | merchant1 | 123456 | 2 |
| 商家 | 陈凯文 | Kv20060426 | 3 |
| 管理员 | icc | Wang2006 | 4 |
| 用户 | user1 | 123456 | 5 |

### 测试酒店

| ID | 名称 | 城市 | 商家ID | 房型数 |
|----|------|------|--------|--------|
| 1 | 北京国际大饭店 | beijing | 2 | 2 |
| 2 | 上海外滩华尔道夫酒店 | shanghai | 2 | 2 |
| 3 | 广州白天鹅宾馆 | guangzhou | 3 | 2 |
| 4 | 深圳瑞吉酒店 | shenzhen | 3 | 2 |

---

## 🎯 初始化后的下一步

### 1. 启动服务

```bash
# 后端
cd backend
npm start

# 前端（新终端）
npm run dev
```

### 2. 访问系统

- 前端: http://localhost:5173
- 后端API: http://localhost:5000

### 3. 测试功能

```bash
# 登录测试
用户名: merchant1
密码: 123456

# 测试功能
1. 查看酒店列表
2. 查看酒店详情
3. 添加收藏
4. 对比酒店
5. 查看数据看板
```

---

## 📚 相关文档

- [新同事快速上手.md](./新同事快速上手.md) - 快速上手指南
- [HANDOVER-GUIDE.md](./HANDOVER-GUIDE.md) - 完整交接指南
- [📢-给下一位同事的信息.md](./📢-给下一位同事的信息.md) - 重要信息

---

## ✅ 初始化检查清单

完成初始化后，请确认：

- [ ] 数据库 `hotel_booking` 已创建
- [ ] 所有基础表已创建（8个表）
- [ ] 测试数据已插入（5用户、4酒店、8房型）
- [ ] AI功能表已创建
- [ ] 所有索引已创建
- [ ] `ai_reason` 字段已添加
- [ ] 数据完整性检查通过
- [ ] 测试账号可以登录
- [ ] 前后端都能正常启动

---

**初始化完成！开始开发吧！** 🚀

**最后更新**: 2026-02-24  
**维护者**: Hotel Booking AI Team

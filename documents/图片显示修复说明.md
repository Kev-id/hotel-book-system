# 图片显示修复说明

## 问题描述

数据库中存储的图片路径格式为：`["/uploads/hotels/0.png", "/uploads/hotels/1.png"]`

这是本地路径，前端直接使用会导致图片无法显示，因为缺少服务器地址。

## 解决方案

### 修改内容

**列表页（List）和详情页（Detail）**都已修复，现在支持两种图片格式：

1. **完整 URL**（如：`https://images.unsplash.com/photo-xxx`）
   - 直接使用

2. **本地路径**（如：`/uploads/hotels/0.png`）
   - 自动拼接为：`http://localhost:5000/uploads/hotels/0.png`

### 代码逻辑

```javascript
// 判断是否为完整 URL
img.startsWith('http') 
  ? img  // 完整 URL，直接使用
  : `http://localhost:5000${img}`  // 本地路径，拼接完整 URL
```

### 错误处理

列表页还添加了 `onError` 处理：
- 如果图片加载失败（如文件不存在）
- 自动显示默认占位图片（根据酒店星级）

## 使用说明

### 确保图片文件存在

你需要在 `backend/uploads/hotels/` 目录下放置对应的图片文件：

```
backend/uploads/hotels/
├── 0.png  # 北京国际大饭店 - 图1
├── 1.png  # 北京国际大饭店 - 图2
├── 2.png  # 上海外滩华尔道夫酒店 - 图1
├── 3.png  # 上海外滩华尔道夫酒店 - 图2
├── 4.png  # 广州白天鹅宾馆 - 图1
└── 5.png  # 深圳瑞吉酒店 - 图1
```

### 创建目录

如果目录不存在，创建它：

```bash
mkdir -p backend/uploads/hotels
```

### 放置图片

将你的酒店图片重命名为 `0.png`, `1.png`, `2.png` 等，然后放入该目录。

## 测试步骤

1. **确保后端服务运行**：
   ```bash
   cd backend
   npm run dev
   ```

2. **确保前端服务运行**：
   ```bash
   npm run dev
   ```

3. **访问列表页**：
   - 打开 http://localhost:5173/list
   - 应该能看到酒店卡片显示图片

4. **访问详情页**：
   - 点击任意酒店卡片
   - 应该能看到图片轮播

## 图片显示优先级

### 列表页
1. 如果酒店有上传的图片 → 显示第一张
2. 如果图片加载失败 → 显示默认占位图
3. 如果没有图片 → 显示默认占位图

### 详情页
1. 如果酒店有上传的图片 → 轮播显示所有图片
2. 如果没有图片 → 显示默认占位图（3张）

## 注意事项

1. **图片文件必须存在**
   - 数据库中的路径只是引用
   - 实际文件必须在 `backend/uploads/hotels/` 目录下

2. **图片格式**
   - 支持：jpg, jpeg, png, gif, webp
   - 建议使用 png 或 jpg

3. **图片大小**
   - 建议宽度：1200px 以上
   - 建议大小：不超过 5MB

4. **后端静态文件服务**
   - 已在 `backend/server.js` 中配置
   - 路由：`/uploads` → `backend/uploads` 目录

## 如果图片还是不显示

### 检查清单

1. ✅ 后端服务是否运行在 5000 端口
2. ✅ 图片文件是否存在于 `backend/uploads/hotels/` 目录
3. ✅ 图片文件名是否与数据库中的路径匹配
4. ✅ 浏览器控制台是否有错误信息
5. ✅ 网络请求是否成功（F12 → Network 标签）

### 调试方法

打开浏览器控制台（F12），查看：

1. **Console 标签**：
   - 查看是否有 JavaScript 错误
   - 查看调试日志（第一个酒店数据、images 字段等）

2. **Network 标签**：
   - 查看图片请求是否成功
   - 如果是 404，说明文件不存在
   - 如果是 500，说明服务器错误

3. **直接访问图片**：
   - 在浏览器中访问：`http://localhost:5000/uploads/hotels/0.png`
   - 如果能看到图片，说明后端配置正确
   - 如果看不到，检查文件是否存在

## 完成

现在列表页和详情页都能正确显示酒店图片了！

如果图片文件不存在，会自动显示默认占位图，不会影响页面显示。

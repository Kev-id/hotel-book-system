# 酒店预订系统 - 项目总结文档

## 📋 项目概述

这是一个基于现代 Web 技术栈开发的全栈酒店预订系统，支持用户浏览酒店、商户管理酒店信息、管理员审核酒店等完整业务流程。

**项目名称**：酒店预订系统 (Hotel Booking System)  
**版本**：1.0.0  
**开发时间**：2024-2026  

---

## 🎯 核心功能

### 1. 用户端功能（移动端优先）
- **首页展示**：酒店列表浏览，支持城市筛选
- **酒店列表**：多条件筛选（城市、价格区间、状态）
- **酒店详情**：查看酒店完整信息（名称、地址、价格、图片、描述）

### 2. 商户端功能
- **用户注册/登录**：支持商户角色注册
- **酒店录入**：新增酒店信息（需审核）
- **酒店编辑**：修改已有酒店信息
- **权限控制**：仅商户角色可访问

### 3. 管理员功能
- **用户注册/登录**：支持管理员角色注册
- **酒店审核**：审核商户提交的酒店信息
- **状态管理**：批准/拒绝酒店上架
- **权限控制**：仅管理员角色可访问

---

## 🛠️ 技术栈

### 前端技术
| 技术 | 版本 | 用途 |
|------|------|------|
| React | 19.2.0 | 前端框架 |
| Vite | 7.2.4 | 构建工具 |
| React Router | 7.13.0 | 路由管理 |
| Ant Design | 6.2.3 | PC端UI组件库 |
| Ant Design Mobile | 5.42.3 | 移动端UI组件库 |
| ESLint | 9.39.1 | 代码规范检查 |

### 后端技术
| 技术 | 版本 | 用途 |
|------|------|------|
| Node.js | - | 运行环境 |
| Express | 4.18.2 | Web框架 |
| MySQL | - | 数据库 |
| mysql2 | 3.6.0 | MySQL驱动 |
| CORS | 2.8.5 | 跨域处理 |
| dotenv | 16.3.1 | 环境变量管理 |
| Nodemon | 3.0.1 | 开发热重载 |

---

## 📁 项目结构

```
hotel-book-system-master/
├── backend/                    # 后端服务
│   ├── config/                # 配置文件
│   │   ├── config.js         # 通用配置
│   │   └── database.js       # 数据库连接
│   ├── controllers/           # 控制器层
│   │   ├── hotelController.js
│   │   └── userController.js
│   ├── routes/               # 路由层
│   ├── sql/                  # 数据库脚本
│   │   └── init.js          # 初始化脚本
│   ├── .env                  # 环境变量
│   ├── server.js            # 服务入口
│   └── package.json         # 后端依赖
│
├── src/                      # 前端源码
│   ├── api/                 # API接口封装
│   │   ├── hotelApi.js
│   │   └── userApi.js
│   ├── components/          # 公共组件
│   │   ├── AuthGuard.jsx   # 权限守卫
│   │   └── Navigation.jsx  # 导航组件
│   ├── context/            # 上下文管理
│   │   └── AuthContext.jsx # 认证上下文
│   ├── pages/              # 页面组件
│   │   ├── admin/         # 管理后台页面
│   │   │   ├── Login/    # 登录页
│   │   │   ├── Register/ # 注册页
│   │   │   ├── HotelForm/# 酒店录入/编辑
│   │   │   └── Audit/    # 酒店审核
│   │   └── client/        # 用户端页面
│   │       ├── Home/     # 首页
│   │       ├── List/     # 列表页
│   │       └── Detail/   # 详情页
│   ├── router/            # 路由配置
│   ├── styles/            # 样式文件
│   ├── App.jsx           # 根组件
│   └── main.jsx          # 入口文件
│
├── public/               # 静态资源
├── index.html           # HTML模板
├── vite.config.js       # Vite配置
├── package.json         # 前端依赖
├── README.md            # 项目说明
└── QUICK_START.md       # 快速启动指南
```

---

## 🗄️ 数据库设计

### users 表（用户表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键，自增 |
| username | VARCHAR(50) | 用户名，唯一 |
| password | VARCHAR(255) | 密码 |
| role | ENUM | 角色：admin/merchant |
| created_at | TIMESTAMP | 创建时间 |

### hotels 表（酒店表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | INT | 主键，自增 |
| name | VARCHAR(100) | 酒店名称 |
| city | VARCHAR(50) | 所在城市 |
| address | VARCHAR(200) | 详细地址 |
| price | DECIMAL(10,2) | 价格 |
| image | VARCHAR(500) | 图片URL |
| description | TEXT | 酒店描述 |
| status | ENUM | 状态：pending/approved/rejected |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |

---

## 🔌 API 接口文档

### 用户相关接口

#### 1. 用户登录
- **接口**：`GET /api/users`
- **参数**：
  - `username`: 用户名
  - `password`: 密码
  - `role`: 角色（admin/merchant）
- **返回**：用户信息对象

#### 2. 用户注册
- **接口**：`POST /api/users`
- **请求体**：
  ```json
  {
    "username": "string",
    "password": "string",
    "confirmPwd": "string",
    "role": "admin|merchant"
  }
  ```
- **返回**：注册成功的用户信息

### 酒店相关接口

#### 1. 获取酒店列表
- **接口**：`GET /api/hotels`
- **查询参数**：
  - `city`: 城市筛选
  - `price_gte`: 最低价格
  - `price_lte`: 最高价格
  - `status`: 状态筛选
- **返回**：酒店列表数组

#### 2. 获取酒店详情
- **接口**：`GET /api/hotels/:id`
- **参数**：`id` - 酒店ID
- **返回**：酒店详情对象

#### 3. 新增酒店
- **接口**：`POST /api/hotels`
- **请求体**：
  ```json
  {
    "name": "string",
    "city": "string",
    "address": "string",
    "price": "number",
    "image": "string",
    "description": "string"
  }
  ```
- **返回**：新增的酒店信息

#### 4. 更新酒店（审核）
- **接口**：`PATCH /api/hotels/:id`
- **请求体**：
  ```json
  {
    "status": "approved|rejected"
  }
  ```
- **返回**：更新后的酒店信息

#### 5. 删除酒店
- **接口**：`DELETE /api/hotels/:id`
- **参数**：`id` - 酒店ID
- **返回**：删除成功消息

---

## 🚀 快速启动

### 环境要求
- Node.js 14+
- MySQL 5.7+
- npm 或 yarn

### 启动步骤

#### 第一步：启动后端服务
```bash
cd backend
npm install          # 安装依赖（首次）
npm run init-db     # 初始化数据库（首次）
npm run dev         # 启动后端服务
```
**成功标志**：控制台显示 `Server is running on port 5000`

#### 第二步：启动前端服务
```bash
cd ..               # 回到项目根目录
npm install        # 安装依赖（首次）
npm run dev       # 启动前端服务
```
**成功标志**：控制台显示 `VITE v... ready in ... ms`

#### 第三步：访问系统
- **前端地址**：http://localhost:5173
- **后端地址**：http://localhost:5000

---

## 👤 测试账号

### 管理员账号
- **用户名**：`admin1`
- **密码**：`123456`
- **权限**：酒店审核

### 商户账号
- **用户名**：`merchant1`
- **密码**：`123456`
- **权限**：酒店录入/编辑

### 开发者账号
- **用户名**：`陈凯文`
- **密码**：`Kv20060426`
- **权限**：商户权限

---

## 🔐 权限控制

### 路由权限
- **公开路由**：首页、列表页、详情页、登录、注册
- **商户路由**：酒店录入/编辑（`/admin/hotel-form`）
- **管理员路由**：酒店审核（`/admin/audit`）

### 实现方式
- 使用 `AuthGuard` 组件包裹需要权限的路由
- 通过 `AuthContext` 管理用户登录状态
- 前端路由拦截 + 后端接口验证

---

## 🎨 UI/UX 设计

### 响应式设计
- **移动端优先**：使用 Ant Design Mobile 组件库
- **PC端适配**：使用 Ant Design 组件库
- **自适应布局**：支持多种屏幕尺寸

### 主要页面
1. **首页**：酒店卡片展示，城市快速筛选
2. **列表页**：高级筛选，价格区间选择
3. **详情页**：大图展示，完整信息呈现
4. **登录/注册**：简洁表单，角色选择
5. **酒店录入**：表单验证，图片上传
6. **审核页面**：列表展示，快速审核

---

## 🔧 配置说明

### 后端配置（.env）
```env
PORT=5000                    # 服务端口
DB_HOST=localhost           # 数据库地址
DB_USER=root               # 数据库用户
DB_PASSWORD=Kv20060426     # 数据库密码
DB_NAME=hotel_booking      # 数据库名称
```

### 前端配置（vite.config.js）
- **代理配置**：所有 `/api` 请求转发到 `http://localhost:5000`
- **端口配置**：前端运行在 `5173` 端口
- **热更新**：支持代码修改后自动刷新

---

## 📊 项目特色

### 1. 前后端分离
- 清晰的职责划分
- 独立的开发和部署
- RESTful API 设计

### 2. 角色权限管理
- 多角色支持（管理员、商户）
- 路由级别权限控制
- 组件级别权限守卫

### 3. 移动端优先
- 响应式设计
- 移动端UI组件
- 触摸友好交互

### 4. 完整的业务流程
- 用户注册登录
- 酒店信息录入
- 审核工作流
- 状态管理

### 5. 开发体验优化
- 热更新支持
- ESLint 代码规范
- 模块化架构
- 清晰的项目结构

---

## 🐛 常见问题

### 1. 数据库连接失败
**解决方案**：
- 检查 MySQL 服务是否启动
- 验证 `.env` 文件中的数据库配置
- 确认数据库用户权限

### 2. 端口被占用
**解决方案**：
- 修改 `.env` 中的 `PORT` 配置
- 或关闭占用端口的进程

### 3. 跨域问题
**解决方案**：
- 已通过 Vite 代理配置解决
- 后端已启用 CORS 中间件

### 4. 依赖安装失败
**解决方案**：
- 清除缓存：`npm cache clean --force`
- 删除 `node_modules` 和 `package-lock.json`
- 重新安装：`npm install`

---

## 📈 未来优化方向

### 功能扩展
- [ ] 用户预订功能
- [ ] 订单管理系统
- [ ] 支付集成
- [ ] 评论评分系统
- [ ] 收藏夹功能
- [ ] 搜索历史记录

### 技术优化
- [ ] 添加单元测试
- [ ] 集成 TypeScript
- [ ] 性能优化（懒加载、缓存）
- [ ] SEO 优化
- [ ] PWA 支持
- [ ] Docker 容器化部署

### 安全增强
- [ ] JWT 令牌认证
- [ ] 密码加密存储
- [ ] SQL 注入防护
- [ ] XSS 攻击防护
- [ ] CSRF 防护
- [ ] 接口限流

---

## 📝 开发规范

### 代码规范
- 使用 ESLint 进行代码检查
- 遵循 React Hooks 最佳实践
- 组件命名采用 PascalCase
- 文件命名采用 camelCase

### Git 提交规范
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式调整
- `refactor`: 代码重构
- `test`: 测试相关
- `chore`: 构建/工具链更新

### 目录规范
- 页面组件放在 `pages/` 目录
- 公共组件放在 `components/` 目录
- API 接口放在 `api/` 目录
- 工具函数放在 `utils/` 目录（如需要）

---

## 👥 项目团队

**开发者**：陈凯文  
**联系方式**：[待补充]  
**项目仓库**：[待补充]

---

## 📄 许可证

本项目仅供学习和研究使用。

---

## 🙏 致谢

感谢以下开源项目：
- React 团队
- Vite 团队
- Ant Design 团队
- Express.js 社区
- MySQL 社区

---

**文档生成时间**：2026年2月6日  
**文档版本**：v1.0.0

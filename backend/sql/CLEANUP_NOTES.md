# SQL脚本清理说明

## 可以删除的文件

### seed-price-data.js

**状态**: 可以安全删除

**原因**:
- 功能已完全整合到 `init.js` 中
- 运行 `init.js` 会自动生成价格测试数据
- 不再需要单独运行此脚本

**如果保留**:
- 作为参考文件，展示价格数据生成逻辑
- 可用于添加额外的测试数据
- 可用于学习和自定义

**删除命令**:
```bash
cd hotel-book-system/backend/sql
rm seed-price-data.js
```

**影响**: 无，不会影响任何功能

## 建议

### 方案1: 保留作为参考（推荐）

**优点**:
- 保留学习价值
- 可用于自定义数据生成
- 不占用太多空间（小文件）

**操作**:
- 在文件顶部添加注释说明（已完成）
- 在README中标记为"参考文件"

### 方案2: 删除

**优点**:
- 减少文件数量
- 避免混淆

**操作**:
```bash
rm backend/sql/seed-price-data.js
```

## 其他文件状态

### 必须保留的文件

- ✅ `init.js` - 主初始化脚本
- ✅ `migrate.js` - 数据库迁移脚本
- ✅ `README.md` - SQL脚本使用说明

### 测试文件

以下测试文件可以根据需要保留或删除：

- `test-bug-fixes.js` - Bug修复测试
- `test-minprice-bug.js` - 最低价格测试
- `test-minprice-correct.js` - 价格逻辑测试
- `test-price-api.js` - API测试

**建议**: 保留在开发环境，生产环境可以删除

## 文档文件

以下文档文件建议保留：

- ✅ `BUG_FIXES.md` - Bug修复文档
- ✅ `VERIFICATION_GUIDE.md` - 验证指南
- ✅ `价格日历功能说明.md` - 功能说明
- ✅ `数据库初始化整合说明.md` - 整合说明
- ✅ `日期选择器功能说明.md` - 日期选择器说明
- ✅ `每晚低至价格说明.md` - 价格逻辑说明

## 清理检查清单

如果决定清理项目，按以下顺序检查：

### 1. 测试文件
- [ ] 确认测试已通过
- [ ] 决定是否保留测试脚本
- [ ] 删除或移动到 `tests/` 目录

### 2. 参考文件
- [ ] 确认功能已整合
- [ ] 决定是否保留作为参考
- [ ] 添加注释说明

### 3. 文档文件
- [ ] 确认文档是否过时
- [ ] 合并重复的文档
- [ ] 更新README索引

### 4. 临时文件
- [ ] 删除 `.log` 文件
- [ ] 删除 `.tmp` 文件
- [ ] 清理 `node_modules`（如果需要）

## 推荐的项目结构

```
backend/
├── sql/
│   ├── init.js                    # 主初始化脚本 ✅
│   ├── migrate.js                 # 迁移脚本 ✅
│   ├── README.md                  # 使用说明 ✅
│   └── seed-price-data.js         # 参考文件 ⚠️ (可选)
├── tests/                         # 测试目录（可选）
│   ├── test-bug-fixes.js
│   ├── test-minprice-bug.js
│   └── test-price-api.js
└── ...
```

## 总结

**seed-price-data.js**:
- ✅ 可以安全删除
- ⚠️ 建议保留作为参考
- 📝 已添加注释说明其用途

**决策建议**:
- 开发环境：保留
- 生产环境：可以删除
- 学习项目：保留

---

更新日期: 2026-02-11
